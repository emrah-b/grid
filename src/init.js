var gridModule = angular.module("grid", []);

window.i18n = {};

var defaultGridOptions = {
   searching : true,
   selectable: true,
   sorting: true,
   paging: true,
   pageSize: 10,
   i18n: 'tr'
}